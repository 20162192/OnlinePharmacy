<view class="item-box" catchtap="goPage" data-id="{{item.id}}">
  <image src="{{item.img}}" class="img" mode="aspectFill"></image>
  <view class="info">
    <view class="name">{{item.name}}</view>
    <view class="price">￥{{item.price}}</view>
  </view>
</view>